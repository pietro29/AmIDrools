rule "rule 3"
when
	eval(true)
then
	System.out.println( "funge da nuovo drl!" );
end

rule "rule position"
when
	$f:Posizione(location==1)
then
	System.out.println( "Sei in " + $f.getCodice());
end
