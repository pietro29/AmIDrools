rule "rule 3"
when
	eval(true)
then
	txtArea.append( "funge da nuovo drl!\n" );
end

rule "rule position"
when
	$f:Position()
then
	txtArea.append( "Sei in " + $f.getCodice() + "\n");
end

rule "battery state"
when
	$f:Battery()
then
	txtArea.append( "Il livello della batteria è " + $f.getLevel() + "%\n");
end